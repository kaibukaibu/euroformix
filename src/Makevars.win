# Require a modern standard (Armadillo current needs >= C++14)
CXX_STD = CXX17

# Preprocessor opts
PKG_CPPFLAGS = -DARMA_USE_CURRENT

# Compiler flags (OpenMP via R’s vars; no duplicate -fopenmp)
PKG_CXXFLAGS = $(SHLIB_OPENMP_CXXFLAGS)

# Linker flags (order matters a bit on Windows)
PKG_LIBS = $(LAPACK_LIBS) $(BLAS_LIBS) $(FLIBS) $(SHLIB_OPENMP_CXXFLAGS)